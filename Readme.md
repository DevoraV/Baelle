# Bälle Interpreter

## Standard functions
```csharp
ball(tennis);

// Printing the Text
// Example:
ball("foo");
```


## Variablen
```csharp
aufgeblasen = const
platt != const

tennis = string
fussball = int
wasserball = float billiard = double
[ tennis ] = tennis[]
{ key: wasserball } = { key: float }
```